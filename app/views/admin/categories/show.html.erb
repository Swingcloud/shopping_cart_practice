<h1>瀏覽類別</h1>
<%= link_to '回到類別首頁', admin_categories_path, class: 'btn btn-default' %>
<%= link_to '編輯', edit_admin_category_path(@category), class: 'btn btn-success' %>
<h2><%= @category.name %></h2>
<p><%= @category.description %></p>